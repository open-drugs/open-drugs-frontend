export interface ISliderOptions {
  minValue: number;
  maxValue: number;
  startInitialValue: number;
  endInitialValue: number;
  step: number;
  buttonOffset: number;
  containerSize: number;
  smooth: boolean;
}
